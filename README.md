# Agente inteligente probabilístico que calcula la probabilidad de que un paciente tenga Dengue o Covid19 según sus sintomas.

El sistema utiliza:

* Python.
* Libreria pandas para leer los archivos csv.
* Libreria sklearn para entrenar el modelo y aplicar Naive Bayes
* Streamlit.

```

---

# Requisitos

Antes de ejecutar el sistema se debe tener instalado:

* Python 3
* pip
* Git (opcional para clonar el repositorio)

---

# Instalación

Clonar el repositorio:

```bash
git clone https://github.com/usuario/Chatbot-IA.git
```

Crear un entorno virtual (recomendado):

```bash
python3 -m venv venv
```

Activar el entorno virtual:

Linux / Mac

```bash
source venv/bin/activate
```

Windows

```bash
venv\Scripts\activate
```

Instalar dependencias:

```bash
pip install -r requirements.txt
```

---

# Autor
* Duete, Facundo Andrés.
* Maidana, Matias Nahuel.
* Risso, Santiago.

Proyecto académico desarrollado para la materia **Inteligencia Artificial**.
